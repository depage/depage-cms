language: php
php:
    - 5.5
    - 5.4

sudo: required

branches:
  only:
      - master

services:
    - docker

before_script:
    - sudo apt-get update
    - sudo apt-get install -y libssh2-1-dev
    - printf "\n" | pecl install channel://pecl.php.net/ssh2-0.12
    - cd Tests

script: make

