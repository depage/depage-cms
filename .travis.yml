language: php
php:
    - 5.5
    - 5.4

sudo: required

branches:
  only:
      - master

services:
    - docker

before_script:
    - sudo apt-get update -qq
    - sudo apt-get install -y -qq libssh2-1-dev libssh2-php
    - cd Tests

script: make

